# BASELINE — 確定仕様とデグレ防止ルール

**このファイルは、AI（タクミ・ジェニー・その他）がこのリポジトリを触る前に必ず読むこと。**

- 基準日: 2026-07-04（ひでっち承認済み）
- 基準コミット: `cd297293fe863801fda2e45be468271b869ed20f`
- 凍結バックアップ: ブランチ `baseline-20260704`（このブランチには絶対にpushしない。復元専用）
- 本番URL: https://asobi-create-park.pages.dev/
- 正しいリポジトリ: `tomohide1224/asobi-create-park`（**asobi-makersは廃止済み。絶対に使わない**）

---

## 確定仕様（トップページ ヒーロー）※ひでっち承認済み・勝手に変えるの禁止

- ヒーローセクション全体を `max-width: 1480px` + `margin: 0 auto` で**ページ中央に配置**（1480px＝カード群と同じ幅基準）
- 画像は `width: 100%; height: auto`（**クロップ・object-fit・aspect-ratio禁止**。全体をそのまま見せる）
- テキストは左46%にオーバーレイ、白グラデーションは**50%で透明**（女の子が見えること）
- 見出し36px・本文14px・CTA16px（1200px以下・1000px以下・768px以下はメディアクエリで縮小）
- スマホ（768px以下）: 画像上（48vh・center top）＋テキスト下
- ファーストビュー方針: ヒーローで画面を埋めきらず、**次のカード（企画/支援）の頭が見える**こと

## 過去に起きたデグレの原因（同じ過ちを繰り返さない）

1. **誤リポジトリへのpush**（asobi-makersにpushし続けて反映されず）
2. **base64二重エンコード**（CSSがbase64文字列として保存された）
3. **push後の本番確認をしなかった**
4. **ブラウザキャッシュ**（直ってもひでっちの画面は古いまま→無駄な再修正でズレが拡大）
5. **意図確認（Intent Check）不足**（「中央にして」の意味を確認せず違う修正を繰り返した）

## 作業ルール（毎回必ず守る）

1. **作業前にこのBASELINE.mdを読む。** 確定仕様に触る変更は、ひでっちにスクショかモックで確認を取ってから。
2. リポジトリ名を確認する: `tomohide1224/asobi-create-park`
3. ファイルは**生テキスト**でpushする（base64エンコード禁止）
4. CSS/JSを変更したら、HTML内のリンクの `?v=` を必ず更新する（例: `?v=20260705`）
5. push後は**必ず本番URLで表示確認**する（確認するまで「完了」と言わない）
6. 変更は依頼された箇所だけ。**関係ないファイル・関係ないセクションは触らない**
7. 大きな変更の前は、その時点のmainからバックアップブランチを切る（例: `baseline-YYYYMMDD`）

## デグレしたときの復元手順

1. `baseline-20260704` ブランチから該当ファイルの内容を取得
2. mainに生テキストでpushして戻す
3. `?v=` を更新して本番確認
